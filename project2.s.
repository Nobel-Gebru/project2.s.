.data
	str: .space 10
	newLine: .asciiz "\n"
	tooLongMessage: .asciiz "Input is too long."
	notInRange: .asciiz "Invalid base-34 number."
	emptyInput: .asciiz "Input is empty."

.globl main

.text
	main:
	
		li $v0, 8
		la $a0, str
		li $a1, 65
		syscall
